<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.cdgeass.TestMapper">

    <sql id="sql1">
    </sql>

    <sql id="sql2">
    </sql>

    <select id="selectOne">
        SELECT
        <include refid="sql1<caret>"/>
        FROM test
    </select>
</mapper>